import 'package:flutter/material.dart';

/// Google's official brand colours for the "G" mark (Sign in with Google
/// branding guidelines): fixed, not derived from [Palette] -- like
/// `logo_colors.dart`, this is a third-party brand identity, not part of
/// our own design system.
const Color googleBlue = Color(0xFF4285F4);
const Color googleGreen = Color(0xFF34A853);
const Color googleYellow = Color(0xFFFBBC05);
const Color googleRed = Color(0xFFEA4335);
