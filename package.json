{
  "name": "nuni",
  "private": true,
  "description": "Node CLIs pinned for reproducible `npx supabase` and `npx vercel` (no Node code in the app).",
  "devDependencies": {
    "supabase": "2.117.0",
    "vercel": "59.17.0"
  }
}
